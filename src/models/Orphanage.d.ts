export default interface Orphanage {
  name: string,
  latitude: number,
  longitude: number,
  about: string,
  instructions: string,
  openingHours: string,
  openOnWeekends: boolean,
  pending: boolean,
};
